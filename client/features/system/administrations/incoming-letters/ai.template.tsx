import Containers from "@/_shared/layouts/components/containers";

export default function AdmIngcomingLettersTemplate() {
  return (
    <Containers
      title="Incoming Letters"
      description={
        "This section used to show incoming letters it will be helpfull if your organizations active to write and send letters"
      }
    >
      <h1>Tar dulu</h1>
    </Containers>
  );
}
